apiVersion: v1
kind: Service
metadata:
  name: cloudnotes-frontend
  namespace: {{ .Release.Namespace | default "default" }}
  labels:
    app: cloudnotes-frontend
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: cloudnotes-frontend
