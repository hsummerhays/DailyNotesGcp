apiVersion: v1
kind: Service
metadata:
  name: cloudnotes-backend
  namespace: {{ .Release.Namespace | default "default" }}
  labels:
    app: cloudnotes-backend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: cloudnotes-backend
